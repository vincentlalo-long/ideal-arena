# Python Starter Kit

Implement `MyStrategy.act()` and `MyStrategy.reset()` in `strategy.py`.
`runner.py` provides the `arena-agent/1` stdio interface for the arena judger
(see [`../README.md`](../README.md)).

```bash
python3 runner.py
```
