# Java Starter Kit

Implement `MyStrategy.act()` and `MyStrategy.reset()` in `MyStrategy.java`.
The runner speaks the `arena-agent/1` stdio protocol (see [`../README.md`](../README.md)).

```bash
javac -d . *.java
java arena.Main
```
